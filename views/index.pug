extends layout

block content
  h1 #{title}
  p Welcome to #{title}
  form(action='/info', method='GET')
    input(type='number', name='routeNumber', placeholder='Num', class='routeNumInput')
    br
    button(type='submit' class='getInfo') Get Info
